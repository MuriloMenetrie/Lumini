Run npm install (you need Node.js for that) to install the development server which will serve the public folder.

Once that command finished, start the server via npm start and view the app at localhost:8081